specVersion: "2.5"
metadata:
  name: ns.migrations
type: application
server:
  customMiddleware:
    - name: fiori-tools-servestatic
      afterMiddleware: compression
      configuration:
        paths:
          - path: /resources/overview
            src: ..\overview\webapp
          - path: /appconfig
            src: appconfig
